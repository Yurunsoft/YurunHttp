language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - 7.3

install:
  - php ./travis/install.php -nproc $(nproc) -version-name $(phpenv version-name)

before_script:
  - phpenv config-rm xdebug.ini
  - composer update

script:
  - composer test
